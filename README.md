**Author** : Ailyn Becerra

1) I would fit my automated tests within a Github action that runs whenever code is pushed. This option is since it make sure that bugs are caught immediately and that tests aren't forgotten, keeping broken code out of the repo.
2) No since E2E tests are meant to simulate a user's actions, not check for logic or correctness.
3) Navigation mode analyzes a page right after it loads and will provide an overall performance metric, but can't analyze interactions or changes in content. Snapshot mode analyzes a page in its current state and is b est used for finding accessibility issues., however it can't analyze JS performance or changes to the DOM tree.
4) We can use smaller image files to speed up loading times. We should also add descriptive "alt" text to images and improve color contrast for better accessibility. Finally, adding better meta descriptions and organizing page headings will help improve search engine optimization.





